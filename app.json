{
  "name": "yt2x",
  "description": "Fetches latest YouTube video & posts to Twitter",
  "scripts": {},
  "env": {
    "X_ACCESS_TOKEN": {
      "required": true
    },
    "X_ACCESS_TOKEN_SECRET": {
      "required": true
    },
    "X_API_KEY": {
      "required": true
    },
    "X_API_KEY_SECRET": {
      "required": true
    },
    "DATABASE_URL": {
      "generator": "secret"
    }
  },
  "addons": [
    "heroku-postgresql",
    "scheduler"
  ],
  "buildpacks": [
    {
      "url": "heroku/ruby"
    }
  ],
  "stack": "heroku-22"
}
